# 👉 Node modules
node_modules/

# 👉 Output di build
dist/
build/
web-build/
.expo/
.expo-shared/

# 👉 Firebase config files (contengono chiavi private)
*.jks
*.p8
*.p12
*.key
*.pem
*.mobileprovision
credentials.json

# 👉 Certificati, provisioning, archive
*.keystore
*.ipa
*.aab
*.apk
*.tar.gz

# 👉 File temporanei e di debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
expo-debug.log
metro-cache/
metro-report/

# 👉 EAS cache
.cache/
builds/

# 👉 TypeScript
*.tsbuildinfo

# 👉 Ambiente locale (evita di committare file .env)
.env
.env.local
.env.*.local

# 👉 macOS & Windows
.DS_Store
Thumbs.db

# 👉 Visual Studio Code
.vscode/

# 👉 Android build (se usi bare workflow o EAS prebuild)
android/.gradle/
android/app/build/
android/build/

# 👉 iOS build (se usi bare workflow o EAS prebuild)
ios/build/
ios/Pods/
ios/*.xcodeproj/
ios/*.xcworkspace/
ios/Podfile.lock

/android
# /ios
